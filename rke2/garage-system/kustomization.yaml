apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - 00-namespace.yaml
  - garage-secret-dev.yaml
  - garage-config.yaml
  - garage-pvcs.yaml
  - garage-statefulset.yaml
  - garage-service.yaml
  - garage-lifecycle-cronjob.yaml
  # Genomics (GEN-03): one-shot Job that creates the khemeia-structures and
  # khemeia-fingerprints buckets and grants them to the existing khemeia-app
  # key. Idempotent; safe to leave applied. See garage-genome-buckets-job.yaml.
  - garage-genome-buckets-job.yaml
