apiVersion: v1
kind: Service
metadata:
  name: alloy
  namespace: monitor
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: alloy
    app.kubernetes.io/instance: alloy
  ports:
  - name: http
    port: 12345
    targetPort: 12345
    protocol: TCP
  - name: syslog
    port: 514
    targetPort: 514
    protocol: UDP
