apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - external-secret.yaml
  - grafana-oidc-secret.yaml
  - prometheus-operator-crds.yaml
  # loki — alloy-values.yaml and loki-values.yaml are Helm values (not K8s manifests)
  - loki/svc-patch-alloy.yaml
  # mikrotik
  - mikrotik/mikrotik-exporter.yaml
  # mikrotik-exporter
  - mikrotik-exporter/mikrotik-exporter.yaml
  # tempo
  - tempo/tempo-config.yaml
  - tempo/tempo-deployment.yaml
  - tempo/tempo-grafana-datasource.yaml
