---
# Same digest-reflection pattern as demo-mcp: the "main" tag is stable,
# so track the digest behind it.
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: voice-agent
  namespace: tooling
spec:
  imageRepositoryRef:
    name: voice-agent
  filterTags:
    pattern: '^main$'
  policy:
    alphabetical:
      order: asc
  digestReflectionPolicy: Always
  interval: 1m0s
