---
# ImageRepository for kai-api.
# Polls zot.hwcopeland.net/kai/kai-api every 5 minutes.
# Credentials from zot-pull-secret (ExternalSecret → bitwarden-login, same pattern as chem).
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: kai-api
  namespace: kai
spec:
  image: zot.hwcopeland.net/kai/kai-api
  interval: 5m
  secretRef:
    name: zot-pull-secret
