apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Note: compute-infrastructure/ is excluded (sub-project with its own git repo)
resources:
  - docking-controller-dnsrecord.yaml
  - docking-controller-httproute.yaml
  - jupyter-deployment.yaml
  - jupyter-dnsrecord.yaml
  - jupyter-httproute.yaml
  - jupyter-pvc.yaml
  - jupyter-service.yaml
