//#region node_modules/molstar/lib/mol-math/interpolate.js
/**
* Copyright (c) 2018 mol* contributors, licensed under MIT, See LICENSE file for more info.
*
* @author Alexander Rose <alexander.rose@weirdbyte.de>
*/
function normalize(value, min, max) {
	return (value - min) / (max - min);
}
function clamp(value, min, max) {
	return Math.max(min, Math.min(max, value));
}
function saturate(value) {
	return clamp(value, 0, 1);
}
function lerp(start, stop, alpha) {
	return start + (stop - start) * alpha;
}
/** Catmul-Rom spline */
function spline(p0, p1, p2, p3, t, tension) {
	const v0 = (p2 - p0) * tension;
	const v1 = (p3 - p1) * tension;
	const t2 = t * t;
	const t3 = t * t2;
	return (2 * p1 - 2 * p2 + v0 + v1) * t3 + (-3 * p1 + 3 * p2 - 2 * v0 - v1) * t2 + v0 * t + p1;
}
function quadraticBezier(p0, p1, p2, t) {
	const k = 1 - t;
	return k * k * p0 + 2 * k * t * p1 + t * t * p2;
}
function smoothstep(min, max, x) {
	x = saturate(normalize(x, min, max));
	return x * x * (3 - 2 * x);
}
//#endregion
//#region node_modules/molstar/lib/mol-math/misc.js
/**
* Copyright (c) 2018-2021 mol* contributors, licensed under MIT, See LICENSE file for more info.
*
* @author Alexander Rose <alexander.rose@weirdbyte.de>
*/
var halfPI = Math.PI / 2;
var PiDiv180 = Math.PI / 180;
function degToRad(deg) {
	return deg * PiDiv180;
}
function radToDeg(rad) {
	return rad / PiDiv180;
}
function isPowerOfTwo(x) {
	return x !== 0 && (x & x - 1) === 0;
}
/** return the value that has the largest absolute value */
function absMax(...values) {
	let max = 0;
	let absMax = 0;
	for (let i = 0, il = values.length; i < il; ++i) {
		const value = values[i];
		const abs = Math.abs(value);
		if (abs > absMax) {
			max = value;
			absMax = abs;
		}
	}
	return max;
}
/** Length of an arc with angle in radians */
function arcLength(angle, radius) {
	return angle * radius;
}
/** Create an outward spiral of given `radius` on a 2d grid */
function spiral2d(radius) {
	let x = 0;
	let y = 0;
	let deltaX = 0;
	let deltaY = -1;
	const size = radius * 2 + 1;
	const halfSize = size / 2;
	const out = [];
	for (let i = Math.pow(size, 2); i > 0; --i) {
		if (-halfSize < x && x <= halfSize && -halfSize < y && y <= halfSize) out.push([x, y]);
		if (x === y || x < 0 && x === -y || x > 0 && x === 1 - y) {
			const prevDeltaX = deltaX;
			deltaX = -deltaY;
			deltaY = prevDeltaX;
		}
		x += deltaX;
		y += deltaY;
	}
	return out;
}
//#endregion
export { isPowerOfTwo as a, clamp as c, quadraticBezier as d, smoothstep as f, halfPI as i, lerp as l, arcLength as n, radToDeg as o, spline as p, degToRad as r, spiral2d as s, absMax as t, normalize as u };

//# sourceMappingURL=misc-D_6jofRh.js.map