//#region node_modules/molstar/lib/mol-util/now.js
/**
* Copyright (c) 2017 mol* contributors, licensed under MIT, See LICENSE file for more info.
*
* @author David Sehnal <david.sehnal@gmail.com>
*/
var now = function() {
	if (typeof window !== "undefined" && window.performance) {
		const perf = window.performance;
		return () => perf.now();
	} else if (typeof process !== "undefined" && process.hrtime !== "undefined" && typeof process.hrtime === "function") return () => {
		const t = process.hrtime();
		return t[0] * 1e3 + t[1] / 1e6;
	};
	else if (Date.now) return () => Date.now();
	else return () => +/* @__PURE__ */ new Date();
}();
function formatTimespan(t, includeMsZeroes = true) {
	if (isNaN(t)) return "n/a";
	const h = Math.floor(t / (3600 * 1e3)), m = Math.floor(t / (60 * 1e3) % 60), s = Math.floor(t / 1e3 % 60);
	let ms = Math.floor(t % 1e3).toString();
	while (ms.length < 3) ms = "0" + ms;
	while (!includeMsZeroes && ms.length > 1 && ms[ms.length - 1] === "0") ms = ms.substr(0, ms.length - 1);
	if (h > 0) return `${h}h${m}m${s}.${ms}s`;
	if (m > 0) return `${m}m${s}.${ms}s`;
	if (s > 0) return `${s}.${ms}s`;
	return `${t.toFixed(0)}ms`;
}
//#endregion
//#region node_modules/molstar/lib/mol-util/uuid.js
/**
* Copyright (c) 2017 mol* contributors, licensed under MIT, See LICENSE file for more info.
*
* @author David Sehnal <david.sehnal@gmail.com>
*/
var UUID;
(function(UUID) {
	const _btoa = typeof btoa !== "undefined" ? btoa : (s) => Buffer.from(s).toString("base64");
	const chars = [];
	/** Creates a 22 characters 'base64' encoded UUID */
	function create22() {
		let d = +/* @__PURE__ */ new Date() + now();
		for (let i = 0; i < 16; i++) {
			chars[i] = String.fromCharCode((d + Math.random() * 255) % 255 | 0);
			d = Math.floor(d / 255);
		}
		return _btoa(chars.join("")).replace(/\+/g, "-").replace(/\//g, "_").substr(0, 22);
	}
	UUID.create22 = create22;
	function createv4() {
		let d = +/* @__PURE__ */ new Date() + now();
		return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, function(c) {
			const r = (d + Math.random() * 16) % 16 | 0;
			d = Math.floor(d / 16);
			return (c === "x" ? r : r & 3 | 8).toString(16);
		});
	}
	UUID.createv4 = createv4;
	function is(x) {
		return typeof x === "string";
	}
	UUID.is = is;
})(UUID || (UUID = {}));
//#endregion
//#region node_modules/molstar/lib/mol-util/id-factory.js
/**
* Copyright (c) 2018 mol* contributors, licensed under MIT, See LICENSE file for more info.
*
* @author Alexander Rose <alexander.rose@weirdbyte.de>
*/
/** Builds id function returning ids within [firstId, maxId) */
function idFactory(firstId = 0, maxId = Number.MAX_SAFE_INTEGER) {
	let _nextId = firstId;
	return () => {
		const ret = _nextId;
		_nextId = (_nextId + 1) % maxId;
		return ret;
	};
}
//#endregion
export { now as i, UUID as n, formatTimespan as r, idFactory as t };

//# sourceMappingURL=id-factory-BB1v2gXS.js.map