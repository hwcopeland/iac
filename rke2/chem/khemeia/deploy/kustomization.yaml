apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - crds/
  - rbac.yaml
  - rbac-crds.yaml
  - compute-classes.yaml
  - vina-gpu-kernel-cache-pvc.yaml
  - plugins-configmap.yaml
  - api-deployment.yaml
  - web-deployment.yaml
  - result-writer.yaml
  - prolif-runner.yaml
  - library-prep.yaml
  - library-prep-keda.yaml
  - admet.yaml
  - postgres.yaml
  - postgres-secret.yaml
  - httproute.yaml
  - flux-webhook-secret.yaml
  - flux-webhook-receiver.yaml
  - servicemonitor.yaml
  - grafana-dashboard-khemeia.yaml
  # Garage moved to rke2/garage-system/ on 2026-05-20 — see that kustomization.
  # Only the local Secret holding S3 credentials stays in chem so the
  # khemeia-controller can resolve them via secretKeyRef.
  - garage-secret.yaml
