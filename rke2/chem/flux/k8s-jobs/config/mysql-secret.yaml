---
# MySQL root password for the docking-mysql instance.
# Internal dev service — ClusterIP only, not externally accessible.
# TODO: migrate to ExternalSecret backed by Bitwarden for production.
apiVersion: v1
kind: Secret
metadata:
  name: docking-mysql-secret
  namespace: chem
type: Opaque
stringData:
  root-password: "Xk9mZ4rD-docking-root"
