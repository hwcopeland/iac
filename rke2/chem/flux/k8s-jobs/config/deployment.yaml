# Deployment for the Docking Job Controller
apiVersion: apps/v1
kind: Deployment
metadata:
  name: docking-controller
  namespace: chem
  labels:
    app.kubernetes.io/name: docking-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: docking-controller
  template:
    metadata:
      labels:
        app.kubernetes.io/name: docking-controller
    spec:
      serviceAccountName: docking-controller
      imagePullSecrets:
        - name: zot-pull-secret
      containers:
        - name: controller
          image: zot.hwcopeland.net/chem/docking-controller:build-000036-9a3d2f1 # {"$imagepolicy": "chem:docking-controller"}
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          env:
            - name: NAMESPACE
              value: "chem"
            - name: MYSQL_HOST
              value: "docking-mysql.chem.svc.cluster.local"
            - name: MYSQL_PORT
              value: "3306"
            - name: MYSQL_USER
              value: "root"
            - name: MYSQL_DATABASE
              value: "docking"
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: docking-mysql-secret
                  key: root-password
            - name: AUTH_ENABLED
              value: "true"
            - name: OIDC_ISSUER_URL
              value: "https://auth.hwcopeland.net/application/o/docking-controller/"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
---
# Service for the Docking Job Controller
apiVersion: v1
kind: Service
metadata:
  name: docking-controller
  namespace: chem
  labels:
    app.kubernetes.io/name: docking-controller
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: docking-controller
