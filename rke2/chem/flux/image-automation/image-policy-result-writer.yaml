---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: result-writer
  namespace: chem
spec:
  imageRepositoryRef:
    name: result-writer
  filterTags:
    pattern: '^build-[0-9]+'
    extract: "$0"
  policy:
    alphabetical:
      order: asc
