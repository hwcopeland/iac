---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: periodic-table
  namespace: web-server
spec:
  parentRefs:
    - name: hwcopeland-gateway
      namespace: kube-system
      sectionName: https
  hostnames:
    - "ptable.hwcopeland.net"
  rules:
    - backendRefs:
        - name: periodic-table
          port: 80
