{
  "name": "periodic-table",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "data:build": "node scripts/prune-data.mjs",
    "predev": "npm run data:build",
    "dev": "vite",
    "prebuild": "npm run data:build",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "pretest": "npm run data:build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.8",
    "jsdom": "^25.0.1"
  }
}
