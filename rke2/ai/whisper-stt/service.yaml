---
apiVersion: v1
kind: Service
metadata:
  name: whisper-stt
  namespace: ai
spec:
  type: LoadBalancer
  selector:
    app: jarvis-stack  # whisper-stt container lives inside the combined pod
  ports:
    - port: 8766
      targetPort: 8766
