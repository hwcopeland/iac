---
apiVersion: v1
kind: Service
metadata:
  name: chatterbox-tts
  namespace: ai
spec:
  type: LoadBalancer
  selector:
    app: jarvis-stack  # chatterbox-tts container lives inside the combined pod
  ports:
    - port: 8765
      targetPort: 8765
